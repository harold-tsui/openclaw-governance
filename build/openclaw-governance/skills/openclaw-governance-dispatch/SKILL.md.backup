---
name: openclaw-governance-dispatch
description: |
  消息分拣与 Skill/Agent 路由核心。
  Activate: 用户消息匹配能力描述时自动加载（description匹配）
  Contains: 意图理解、类型分类、Skill路由、Agent路由、Task判断
metadata:
  version: "2.14.0"
  level: "L1"
  os: ["darwin", "linux"]
  refactors:
    - "2026-04-19 v2.14.0: N4-P1-T07 T02 补完 — §三.5 生命周期引导模板（任务完成/阻塞/检查范围/归档位置引导）；§3.5/§3.6 节号顺延"
    - "2026-04-18 v2.13.0: §3.6 nucleus 从 L4 降为 L3（执行引擎非元治理）；L3 行新增 nucleus；更新脚注 ⁵"
    - "2026-04-18 v2.12.0: N4-P1-T07 T02 — 分层引导架构（§1.4 Harold 三个"不能"+§1.5 分层引导+§1.6 dispatch-state.json 模板）；新增 §三.4.1/.2/.3 引导模板；§四.1 fallback 引用分层引导"
    - "2026-04-08 v2.10.0: §2.1 移除 infra_check 意图路由（infrastructure 仅 CTO 角色内部调用）；§3.4 删除 L4-sub 行、nucleus 归入 L4 element、infrastructure 补充 CTO 角色限制说明、evolution 依赖链去掉 infrastructure"
    - "2026-04-08 v2.9.0: §3.4 补充元治理层（evolution独立分类）+ nucleus L4-sub；§2.1意图映射表补充nucleus路由说明脚注"
    - "2026-04-08 v2.8.0: 修复版本号尾部错误；§1.0 projects.yaml→配置文件名修正；§1.2重复编号修复；意图映射表补充4个Skill；版本历史补全2.6.0/2.7.0"
    - "2026-04-07 v2.7.0: 增加核心定义（Task/Topic/Project）、Task判断标准"
    - "2026-04-02 v2.6.0: §三补充缓存检查、异常处理"
    - "2026-04-02 v2.5.0: §二+§三合并，§六精简，示例2个"
---

# OpenClaw Dispatch - 意图理解与路由

Tags: #governance, #dispatch, #routing, #intent, #agent-routing

> **核心职责**：理解用户意图 → 判断是否需要 Task → 路由到正确的 Agent/Skill
> **v2.12.0**（N4-P1-T07 T02）：分层引导架构（归属验证 → Project选择 → 任务类型）；嵌入 Harold 三个"不能"约束；新增 dispatch-state.json 模板

---

## 一、核心定义

### 1.0 Task/Topic/Project 定义

> **定义位置**：本 Skill 是意图识别入口，所有层级定义在此统一管理。

| 层级 | 定义 | 示例 | 归属要求 |
|------|------|------|----------|
| **Project** | 独立目标、资源和时间线的工作单元 | ZT-P001 抖音 AI 书店 | 必须在 system-projects.yaml 或 user-projects.yaml 中注册 |
| **Topic** | Project 下的专题工作单元 | T01 市场调研 | 必须属于某个 Project |
| **Task** | Topic 下的具体执行项 | T01-T01 竞品分析报告 | 必须属于某个 Topic |

**孤儿 Task 处理**：
- 无法归属的 Task 放入 `ZT-P-AGT-{agent}`（Agent 个人工作台）
- 临时性任务放入 `ZT-P000`（NUCLEUS 治理基础）

### 1.1 Task 判断标准

> **核心原则**：并非所有对话都需要创建 Task。

| 条件 | 是否创建 Task |
|------|--------------|
| 需要 Harold 审批/Review | ✅ 必须创建 |
| 需要跨 Agent 协作 | ✅ 必须创建 |
| 工作量 > 30 分钟 | ✅ 必须创建 |
| 会产生交付物 | ✅ 必须创建 |
| 只是简单问答/聊天 | ❌ 不创建 |
| 临时性、一次性操作 | ❌ 不创建 |

**不创建 Task 的场景示例**：
- "今天天气怎么样？"
- "帮我查一下 xxx 的定义"
- "谢谢"
- 简单的文件读取

### 1.2 核心原则

> **所有生产落地方案必须通过 task-card 执行**

| 类型 | 定义 | 需要 task-card |
|------|------|----------------|
| **生产工作** | 产生交付物（写文件、发消息、创建项目等） | ✅ 必须 |
| **非生产** | 闲聊、查询、确认、讨论 | ❌ 不需要 |

### 1.3 阻断机制

```
用户消息 → 意图识别
    ↓
if 生产工作 AND 无 task-card:
    返回错误: "生产工作必须有 task-card，请先创建"
```

### 1.4 Harold 三个"不能"约束 ⭐ v2.12.0

> **来源**：Harold Tsui 2026-04-15 指令，dispatch 作为治理入口必须强制执行

| 约束 | 规则 | 违反后果 |
|------|------|---------|
| **❌ 不能有未知 Project** | 所有 Project 必须在 `system-projects.yaml` 或 `user-projects.yaml` 中注册 | 引导用户确认是否创建新 Project（需 Harold 审批） |
| **❌ 不能有孤儿 Task-CARD** | Task 必须归属某个 Topic | 引导用户选择归属 Project + Topic |
| **❌ 不能有孤儿 Topic** | Topic 必须归属某个 Project | 引导用户选择归属 Project |

**执行逻辑**：
```
if 用户意图涉及创建/修改 Project/Topic/Task:
    1. 验证 Project 是否存在于 projects.yaml
    2. 验证 Topic 是否归属于有效 Project
    3. 验证 Task 是否归属于有效 Topic
    ↓
    任一验证失败 → 触发分层引导（§1.5）
```

### 1.5 分层引导架构 ⭐ v2.12.0

> **核心原则**：不明确归属才引导，明确归属直接执行

```
┌─────────────────────────────────────┐
│ 层级 1：顶层引导（Project/Task归属验证）│ ← 最重要：三个"不能"
├─────────────────────────────────────┤
│ 层级 2：已有Project/Topic选择引导     │ ← 列出候选，避免未知
├─────────────────────────────────────┤
│ 层级 3：任务类型引导（如果需要）       │ ← 交付物/方法论/改进
└─────────────────────────────────────┘
```

**引导触发条件**：

| 用户消息模式 | 触发层级 | 引导模板 |
|-------------|---------|---------|
| "创建一个新项目" | 层级 1 | §三.4.1 创建Project引导 |
| "创建一个新任务" | 层级 1 | §三.4.2 创建Task引导 |
| "处理一下文档"（意图模糊） | 层级 1 | §三.4.3 通用意图引导 |
| 归属明确 | 不引导 | §三.5 显式声明 → 执行 |

### 1.6 dispatch-state.json 状态模板 ⭐ v2.12.0

> **路径**：`.system/governance/current/dispatch-state.json`
> **用途**：记录 dispatch 运行状态，支持状态感知路由

```json
{
  "last_intent": null,
  "last_routed_skill": null,
  "last_declared_action": null,
  "pending_user_selection": null,
  "explicit_declaration": {
    "last_user_declaration": null,
    "parsed_object_id": null,
    "parsed_object_full_name": null,
    "agent_response": null,
    "user_correction": null
  }
}
```

**字段说明**：

| 字段 | 用途 | 更新时机 |
|------|------|---------|
| `last_intent` | 最后一次识别的意图 | 每次意图识别后 |
| `last_routed_skill` | 最后一次路由的 Skill | 路由成功后 |
| `last_declared_action` | 最后一次显式声明的动作 | 显式声明后 |
| `pending_user_selection` | 等待用户选择的引导选项 | 触发引导时设置，用户选择后清空 |
| `explicit_declaration.*` | 显式声明的完整记录 | 每次显式声明后更新 |

### 1.7 输入输出

**输入**：用户消息（文本、附件、引用、上下文）

**输出**：

```json
{
  "intent": "create_project",
  "type": "task",
  "is_production": true,
  "has_task_card": true,
  "skill": "governance-hierarchy",
  "agent": "main",
  "delegation": { "level": "L2", "needs_harold_approval": false },
  "blocked": false
}
```

---

## 二、意图分类与路由表

> **v2.5.0 重构**：一张表解决"识别"和"路由"

### 2.1 意图映射表

| 意图类型 | 关键词示例 | 路由目标 Skill |
|----------|------------|----------------|
| `create_project` | 创建项目、新建项目 | governance-hierarchy |
| `create_task` | 创建任务、新建任务、分派任务 | governance-task |
| `create_topic` | 创建 Topic、新建主题 | governance-hierarchy |
| `quality_review` | 审核、验收交付物、质量检查 | governance-quality |
| `data_classify` | 数据分级、路径校验 | governance-data |
| `delegation_check` | 授权检查、Review 级别判定、降级申请 | governance-delegation ¹ |
| `heartbeat_check` | heartbeat、日报、巡检 | governance-heartbeat |
| `risk_assess` | 风险评估、风险识别、风险登记 | governance-risk |
| `pipeline_run` | 执行流水线、启动 pipeline、阀点管理 | governance-pipeline |
| `evolution_eval` | 系统评估、Skill 进化、治理改进 | governance-evolution ² |
| `chat` | 你好、谢谢、再见 | 不加载 Skill，直接回复 |

> ¹ `delegation_check` 路由说明：`check_route_permission()` 是 governance-delegation 的内部函数，由 dispatch 在路由决策前**主动调用**（见 §三 3.1），不作为独立的意图路由目标。`delegation_check` 意图指用户**主动发起**的授权查询/Review 级别判定请求，路由到 governance-delegation 的 `determine_review()` 或 `check_authorization()` 函数。
>
> ² `evolution_eval` 路由说明：dispatch 只路由到 `governance-evolution`（策略层），**不直接路由到 `governance-nucleus`**。nucleus 是 evolution 的执行引擎子模块，由 evolution 按需启动，或由 governance-heartbeat cron 后台自动触发，不接受 dispatch 直接路由。
>
> ³ `governance-infrastructure` 不出现在意图映射表中。该 Skill 由 CTO 角色内部维护调用，不对普通 Agent 会话暴露入口。

### 2.2 识别规则

```
用户消息到达
    ↓
匹配关键词 → 确定意图类型
    ↓
if 闲聊/问候/感谢:
    直接回复
elif 匹配到意图:
    检查是否需要 task-card
    调用 governance-delegation.check_route_permission()
    加载目标 Skill
else:
    询问澄清
```

---

## 三、路由执行流程

### 3.1 标准流程

```
用户消息到达
    ↓
匹配关键词 → 确定意图类型
    ↓
检查 Skill 是否已加载（缓存）
    ├─ 已加载 → 直接路由
    └─ 未加载 → 触发加载
    ↓
调用 governance-delegation.check_route_permission()
    ↓
加载目标 Skill
    ↓
执行任务
```

### 3.2 缓存检查

> 同一会话内 Skill 已加载则直接复用，避免重复加载。

### 3.3 异常处理（引导选择 + 显式声明） ⭐ v2.11.0

> **核心改进**: 意图不清晰时，提供 A/B/C/D 选项引导用户选择；意图明确时，显式声明执行动作

| 场景 | 处理 |
|------|------|
| Skill 加载失败 | 返回友好提示，降级到 main 处理 |
| **意图歧义** | **引导选择（提供 A/B/C/D 选项 + 记录到 dispatch-state.json）** |
| **明确归属** | **显式声明执行动作（完整对象名 + 执行动作）** |
| 完全无法识别 | **引导澄清（提供通用选项模板）** |

### 3.4 引导机制 ⭐ v2.11.0

> **11 种引导类型**：创建引导(3) + 生命周期引导(4) + 选择引导(2) + 范围确认(1) + 归档引导(1)

**引导触发条件**：
- 不明确归属 → 引导（提供 A/B/C/D 选项）
- 明确归属 → 不引导，显式声明后执行

**引导流程**：
```
用户消息到达
    ↓
意图识别
    ↓
判断归属是否明确？
    ├─ 明确 → 显式声明 → 执行
    └─ 不明确 → 引导选择 → 用户选择 → 显式声明 → 执行
```

**引导类型清单**：

| 引导类型 | 优先级 | 触发关键词 | 选项数量 | 章节 |
|----------|--------|-----------|----------|------|
| **① 创建 Project 引导** | **P0** ⭐ | 创建项目/新建项目/立项 | 3 个（A/B/C） | §三.4.1 |
| **② 执行 Task 引导** | **P0** ⭐ | 帮我完成任务/继续工作 | 3 个（A/B/C） | §三.4.2 |
| **③ 创建 Topic 引导** | **P0** ⭐ | 创建专题/新建 Topic | 3 个（A/B/C） | §三.4.4 |
| **④ 通用意图引导** | **P1** | 帮我处理/处理一下 | 3 个（A/B/C） | §三.4.3 |
| **⑤ 任务完成→下一步** | **P1** | 做完了/任务完成 | 4 个（A/B/C/D） | §三.5.1 |
| **⑥ 任务阻塞→处理** | **P0** ⭐ | 卡住了/阻塞了 | 4 个（A/B/C/D） | §三.5.2 |
| **⑦ 检查范围引导** | **P2** | 检查状态/看看进度 | 3 个（A/B/C） | §三.5.3 |
| **⑧ 归档位置引导** | **P3** | 归档这个任务 | 3 个（A/B/C） | §三.5.4 |
| **⑨ Project 选择引导** | **P1** | 在已有项目上创建 | ≤ 5 个 | §三.4.5 |
| **⑩ Topic 选择引导** | **P1** | 选择 Topic | ≤ 5 个 | §三.4.6 |
| **⑪ 任务类型引导** | **P2** | 归属明确但类型不清 | 4 个（A/B/C/D） | §三.4.7 |

详细设计文档：`10_Projects/ZT-P015_NUCLEUS-4-0/topics/N4-P1-T07/outputs/project-lifecycle-guidance-complete.md`

#### 三.4.1 创建 Project 引导模板 ⭐ v2.12.0

```
我理解您想创建新项目。请确认：

A. 在已有项目基础上创建新 Task/Topic（推荐，避免重复）
B. 确实需要创建新 Project（需要 Harold 审批）
C. 只是查询/讨论，不需要创建

回复 A/B/C 或描述具体需求。
```

**用户选择后的处理**：
- A → 读取 `user-projects.yaml`，列出活跃 Project 供选择
- B → 提示需要 Harold 审批，创建 charter 后暂停
- C → 进入查询/讨论模式

#### 三.4.2 创建 Task 引导模板 ⭐ v2.12.0

```
请确认 Task 归属：

A. 选择已有 Project + Topic（推荐）
B. 放入临时工作台（ZT-P-AGT-{agent}）
C. 放入 NUCLEUS 治理基础（ZT-P000）

回复 A/B/C 或描述具体归属。
```

**用户选择后的处理**：
- A → 读取 `user-projects.yaml`，列出活跃 Project 及其 Topic
- B → 允许创建，标记为"待归属"，提示后续需要正式归属
- C → 允许创建到 ZT-P000

#### 三.4.3 通用意图引导模板 ⭐ v2.12.0

```
我理解您想{概括用户意图}，但需要确认具体操作：

A. {最可能的操作}
B. {次可能的操作}
C. 其他（请描述）

回复 A/B/C 或描述具体需求。
```

**示例**：用户说"帮我处理一下文档"
```
我理解您想处理文档，但需要确认具体操作：

A. 创建新文档（需要先创建 Task）
B. 更新已有文档（请指定文档路径或名称）
C. 其他（请描述具体需求）

回复 A/B/C 或描述具体需求。
```

#### 三.4.4 创建 Topic 引导 ⭐ v2.14.0

**触发条件**：用户说"创建一个新Topic"/"新建专题"，归属不明确

```
我理解您想创建新 Topic。请确认归属：

A. 选择已有 Project（推荐）
B. 放入我的临时工作台（ZT-P-AGT-{agent}）
C. 放入 NUCLEUS 治理基础（ZT-P000）

回复 A/B/C 或描述具体归属。
```

**用户选择后的处理**：
- A → 进入 Project 选择引导（§三.4.5）
- B/C → 允许创建，标记为"待归属"

#### 三.4.5 Project 选择引导 ⭐ v2.14.0

**触发条件**：用户在层级 1 选择"选择已有 Project"

```
请选择已有 Project：

1. ZT-P015 NUCLEUS 4.0（NUCLEUS 灵枢计划）
2. ZT-P009 NUCLEUS 2.0（治理体系）
3. ZT-P-AGT-cqo（张铁个人工作台）
4. 其他（列出更多）

回复数字或 Project 名称。
```

**数据源**：`system-projects.yaml` + `user-projects.yaml`，仅列出 `status: active` 的 Project。

#### 三.4.6 Topic 选择引导 ⭐ v2.14.0

**触发条件**：用户选择 Project 后，需要选择 Topic

```
请选择 {Project 名称} 下的 Topic：

1. {Topic 1 名称}
2. {Topic 2 名称}
3. 其他（列出更多）

回复数字或 Topic 名称。
```

**数据源**：扫描 `10_Projects/{Project ID}/topics/` 目录下的 TOPIC-BRIEF.md 文件。

#### 三.4.7 任务类型引导 ⭐ v2.14.0

**触发条件**：Project + Topic 归属明确，但任务类型不明确

```
已确认归属：{Project ID}/{Topic ID}

请选择任务类型：

A. 创建新 Task-CARD
B. 执行已有 Task
C. 查看 Task 状态
D. 修改 Task

回复 A/B/C/D 或描述具体需求。
```

### 3.5 生命周期引导模板 ⭐ v2.14.0

> **来源**：N4-P1-T07 T02 补完 — 任务完成/阻塞/检查/归档引导

#### 三.5.1 任务完成→下一步引导

**触发条件**：用户说"任务完成了"/"做完了"

```
任务已完成。请选择下一步：

A. 创建后续任务（继承当前 Project/Topic 归属）
B. 更新 MISSION_BOARD（状态标记为已完成）
C. 归档任务（移动到 archive 目录）
D. 暂无后续动作

回复 A/B/C/D 或描述具体需求。
```

**用户选择后的处理**：
- A → 自动继承 Project + Topic，进入创建 Task 引导
- B → 定位对应 Task-CARD，更新状态为 `[x]`
- C → 进入归档位置引导（§三.5.4）
- D → 记录完成，静默

#### 三.5.2 任务阻塞→处理方式引导

**触发条件**：用户说"任务阻塞了"/"卡住了"/"无法继续"

```
任务已阻塞。请选择处理方式：

A. 上报 Harold（需要 Harold 决策）
B. 自行解决（尝试自己解决阻塞）
C. 等待外部条件（等待他人/外部条件）
D. 暂时搁置（标记为阻塞，稍后处理）

回复 A/B/C/D 或描述具体需求。
```

**用户选择后的处理**：
- A → 触发 P0 上报流程，通过飞书通知 Harold
- B → 记录阻塞原因，继续执行
- C → 标记 Task 为 `[~]` 阻塞状态，设置下次检查时间
- D → 标记 Task 为 `[~]`，加入后续 Heartbeat 追踪

#### 三.5.3 检查范围引导

**触发条件**：用户说"检查任务状态"/"看看进度"，范围不明确

```
请确认检查范围：

A. 全局检查（所有 Project/Task）
B. 单项目检查（请指定 Project）
C. 单 Task 检查（请指定 Task ID 或名称）

回复 A/B/C 或描述具体需求。
```

#### 三.5.4 归档位置引导

**触发条件**：用户说"归档这个任务"，归档位置不明确

```
请确认归档位置：

A. Project archive 目录（10_Projects/ZT-PXXX/archive/）
B. Topic archive 目录（topics/N4-P1-T07/archive/）
C. 按日期归档到全局 archive（.system/governance/archive/）

回复 A/B/C 或描述具体需求。
```

**默认推荐**：优先选择 A（Project archive），保持 Project 完整性。

### 3.6 显式声明规范 ⭐ v2.11.0

> **Harold 原则**: 对于任何明确的、紧急的情况，都需要显式声明你的选择，让用户知道你的选择是什么。

**显式声明格式**：
```
正在执行：{完整对象名称}（{补充信息}）

示例：
正在关闭：ZT-P015 NUCLEUS 4.0 项目（Harold 审批流程）
正在执行：N4-P1-T07-T02 Dispatch 引导入口设计（状态：执行中）
正在查询：ZT-P015/N4-P1-T07 的 Task 列表（共 3 个 Task）
```

**核心价值**：
- 用户看到完整信息 → 发现说错了 → 立即纠正
- 避免 Agent 理解偏差 → 避免"关闭了错误的项目"

**状态文件关联**：
```json
{
  "explicit_declaration": {
    "last_user_declaration": "关闭 P015",
    "parsed_object_id": "ZT-P015",
    "parsed_object_full_name": "ZT-P015 NUCLEUS 4.0",
    "agent_response": "正在关闭：ZT-P015 NUCLEUS 4.0 项目",
    "user_correction": null
  }
}
```

详细设计文档：`10_Projects/ZT-P015_NUCLEUS-4-0/topics/N4-P1-T07/outputs/explicit-declaration-design.md`

### 3.7 Skill 加载层级（对齐 governance-core v6.1.3）

> **对齐 governance-core v6.1.3**：依赖驱动加载，Phase 1-4 启动序列

| 层级 | 代表 Skill | 加载方式 | 触发类型 |
|------|-----------|----------|----------|
| **L1** | core | 会话启动时加载 | 启动触发 |
| **L2 启动触发** | dispatch, config | 会话启动时加载 | 启动触发 |
| **L2 意图触发** | task, hierarchy | 用户消息匹配时加载 | 意图触发 |
| **L2 定时触发** | heartbeat | 周期性加载 | 定时触发 |
| **L3** | quality, data, delegation, risk, nucleus | 按需加载 | 事件/意图/heartbeat 触发 |
| **L4** | pipeline | 惰性加载 | 显式触发 |
| **L4** | evolution | 惰性加载，跨会话系统级 ⁴ | 显式触发 / heartbeat cron |

> ⁴ `governance-evolution`（元治理层）：管理整个 Skill 体系的生命周期（评估、升级、淘汰）。依赖链：core → hierarchy → quality → config → evolution。职责跨越单次会话，属于系统级持续运行能力。
>
> ⁵ `governance-nucleus`（L3 执行引擎）：PDCA 自动执行引擎，有实际 Python 脚本和状态管理，由 heartbeat 定时调用。dispatch 不直接路由到 nucleus。
>
> ⁶ `governance-infrastructure` / `openclaw-governance-intelligence` / `openclaw-governance-content` / `openclaw-governance-visual` 已从治理家族移出，迁移至 `skills/nucleus-tools/`，不属于 dispatch 治理路由。

### 3.7 Agent 路由

由 Skill 的 `agent` 字段决定。

---

## 四、路由层 Fallback 规则

> **v2.5.0 重构**：只保留路由层 fallback，执行层错误处理下沉各 Skill

### 4.1 模糊意图

| 场景 | 处理 |
|------|------|
| 意图不明确 | **分层引导**（§1.5 → §三.4 模板） |
| 多个意图 | 取最高置信度，列出备选（A/B/C 引导） |
| 完全无法识别 | 通用意图引导模板（§三.4.3） |

### 4.2 无法路由

| 场景 | 处理 |
|------|------|
| 无匹配 Skill | 默认 main 处理 |
| 无匹配 Agent | 默认 main 处理 |

---

## 五、示例

### 示例 1：清晰意图

> 用户：「帮我创建一个新项目，做抖音书店」

```yaml
intent: create_project
type: task
skill: governance-hierarchy
agent: main
blocked: false
```

### 示例 2：生产工作无 task-card 阻断

> 用户：「帮我写个文件」

```yaml
intent: write_file
type: task
is_production: true
has_task_card: false
blocked: true
block_reason: "生产工作必须有 task-card，请先创建"
```

---

## 六、版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| **2.14.0** | 2026-04-19 | N4-P1-T07 T02 补完 — §三.5 生命周期引导模板（完成→下一步/阻塞→处理/检查范围/归档位置） |
| **2.13.0** | 2026-04-18 | §3.6 nucleus 从 L4 降为 L3（实际为执行引擎，非元治理）；L3 行新增 nucleus；更新脚注 ⁵ |
| **2.12.0** | 2026-04-18 | N4-P1-T07 T02 — 分层引导架构；§三.3 异常处理改为引导选择；新增 §三.4/.4.1/.4.2/.4.3 引导模板 |
| **2.10.0** | 2026-04-08 | ① §2.1 移除 `infra_check` 意图路由条目，补充脚注 ³ 说明 infrastructure 仅 CTO 角色内部调用；② `pipeline_run` 关键词补充"阀点管理"；③ §3.4 删除 L4-sub 行，nucleus/evolution/infrastructure/pipeline 统一归为 L4，各附脚注区分语义；④ evolution 依赖链去掉 infrastructure |
| **2.9.0** | 2026-04-08 | §3.4 补充元治理层（evolution 独立分类）+ nucleus L4-sub；§2.1 补充 nucleus 路由说明脚注 |
| **2.8.0** | 2026-04-08 | footer 版本号修正；§1.0 配置文件名修正；§1.2 重复编号修复；意图映射表补充 4 个 Skill；版本历史补全 |
| **2.7.0** | 2026-04-07 | 新增核心定义（Task/Topic/Project §1.0）、Task 判断标准（§1.1） |
| **2.6.0** | 2026-04-02 | §三补充缓存检查（§3.2）、异常处理（§3.3） |
| **2.5.0** | 2026-04-02 | §二+§三合并，§六精简，示例精简为 2 个 |
| **2.4.0** | 2026-04-02 | §九权限规则移入 governance-delegation，§七置信度移出 |
| **2.3.0** | 2026-03-24 | 新增显式 Dispatch 触发加载规则 |
| **2.0.0** | 2026-03-16 | 初始版本 |

---

*版本: 2.14.0 | 更新: 2026-04-19 | 变更: 11 种引导类型全量实现（N4-P1-T07 T02 补完）*

## Related Skills
- [[openclaw-governance-core]] - 核心运行机制，路由的基础
- [[openclaw-governance-task]] - 任务管理，Task判断和路由
- [[openclaw-governance-hierarchy]] - 层级管理，Project/Topic路由
- [[openclaw-governance-delegation]] - 授权与等级判定，权限路由