# TASK-CARD · T5.2

> **文件性质**：Task 执行卡片
> **存放路径**：`${OPENCLAW_LOCAL_WORKSPACE}/10_Projects/ZT-P015_NUCLEUS-4-0/tasks/T5.2_TASK-CARD.md`
> **版本**：v3.1
> **Task ID**：T5.2
> **Task 名称**：Plan 模块实现
> **负责人**：张铁 (CQO)
> **状态**：✅ 已完成
> **完成日期**：2026-04-05

---

## 一、Task 概要

| 字段 | 内容 |
|------|------|
| **Task ID** | T5.2 |
| **Task 名称** | Plan 模块实现 |
| **负责人** | 张铁 (CQO) |
| **优先级** | P0 |
| **状态** | 🔄 进行中 |
| **Review 级别** | L3（Harold 全量 Review） |
| **依赖** | T0.2, T1 |

---

## 二、Task 目标

实现 Plan 模块，负责 PDCA 循环的规划阶段：

1. **加载 Task-Card**：从 `task_card_path` 加载验收标准
2. **调用 determine_review()**：根据 protocol v1.3 §3 计算 review_context
3. **初始化 CycleUnit.plan**：写入时间约束（不含验收策略）
4. **支持子环约束**：传递 `parent_review_level` 约束

---

## 三、核心接口

### 3.1 determine_review() 调用

```python
def determine_review(
    task_card_path: str,
    task_id: str,
    scope: str,
    parent_review_level: Optional[str] = None
) -> Dict[str, Any]:
    """
    根据 pdca-check-protocol v1.3 §3 计算 review_context
    
    Args:
        task_card_path: Task-CARD 文件路径
        task_id: Task ID
        scope: 执行粒度
        parent_review_level: 父环 review_level（子环约束）
    
    Returns:
        review_context (内存，不持久化):
        {
            "review_level": "L0|L1|L2|L3",
            "human_approval_required": bool,
            "reviewer": "harold|yin-yue|null",
            "parent_constraint_applied": bool
        }
    """
```

### 3.2 CycleUnit.plan 初始化

```yaml
plan:
  task_card_id: "T5.2"
  task_card_path: "tasks/T5.2_TASK-CARD.md"  # v1.2 继承
  human_approval_required: false             # 从 review_context 初始化
  time_horizon_cycles: 3                     # 时间约束
  max_cycles: 10                             # 循环上限
```

---

## 四、交付物

- [x] `modules/plan.py`（Plan 模块实现）
- [x] `modules/test_plan.py`（单元测试）
- [x] determine_review() 实现验证
- [x] 子环约束验证
- [x] TASK-CARD 状态更新

---

## 五、验收标准

| 标准 | 状态 | 说明 |
|------|------|------|
| **DoD-1** | ✅ | `determine_review()` 正确读取 task-card §一 + §五 |
| **DoD-2** | ✅ | review_context 不写入 cycle_unit（内存） |
| **DoD-3** | ✅ | `human_approval_required` 正确初始化到 cycle_unit.plan |
| **DoD-4** | ✅ | 子环约束 `max(parent_review_level - 1, L0)` 生效 |
| **DoD-5** | ✅ | L0=免审，L3=全量（级别定义正确） |
| **DoD-6** | ✅ | 单元测试覆盖率 ≥ 80%（7/7 passed） |

---

## 六、执行流程

### Step 1：读取 protocol v1.3 §3 接口规范（当前）

确认：
- 输入参数（§3.2）
- 读取逻辑（§3.3）
- 输出格式（§3.4）
- reviewer 选择规则（§3.6）
- 校验规则（§3.7）

### Step 2：实现 determine_review()

```python
# modules/plan.py

def determine_review(...) -> Dict[str, Any]:
    # 1. 加载 task-card
    # 2. 读取 §一 Review 级别
    # 3. 读取 §五 acceptance_criteria
    # 4. 应用子环约束（如有）
    # 5. 计算 reviewer
    # 6. 返回 review_context
```

### Step 3：实现 init_plan()

```python
def init_plan(cycle_id: str, scope: str, task_card_path: str) -> None:
    # 1. 调用 determine_review()
    # 2. 初始化 cycle_unit.plan
    # 3. save_cycle()
```

### Step 4：单元测试验证

- task-card 加载正确
- review_level 计算正确
- 子环约束生效
- L0/L3 级别定义正确

### Step 终：闭环

1. 更新 T5.2 TASK-CARD 状态 ✅
2. 更新 TOPIC-BRIEF Task 列表 ✅
3. 测试覆盖率 ≥ 80% ✅

---

## 七、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| task-card §一/§五 格式不一致 | 读取失败 | Schema 校验 + 测试验证 |
| 子环约束计算错误 | 级别失控 | protocol §3.2 + §3.7 对齐 |

---

## 八、完成记录

| 日期 | 事件 | 说明 |
|------|------|------|
| 2026-04-05 | T5.2 启动 | 创建 TASK-CARD |
| 2026-04-05 | plan.py 实现 | determine_review() + init_plan() |
| 2026-04-05 | 单元测试通过 | 7/7 passed |
| 2026-04-05 | T5.2 完成 | 所有 DoD 达成 |

---

## 九、知识沉淀

### 9.1 LESSON-LEARNED

1. **[LL-001]** 规划阶段需要加载任务卡片：从 task_card_path 加载验收标准，确保计划符合任务要求
2. **[LL-002]** 评审级别计算需要考虑子环约束：传递 parent_review_level 约束，确保子环的评审级别不高于父环
3. **[LL-003]** 单元测试覆盖重要场景：覆盖 task-card 加载、review_level 计算、子环约束生效和 L0/L3 级别定义

### 9.2 DECISION-LIBRARY

1. **[DL-001]** 评审级别定义：L0=免审，L3=全量
2. **[DL-002]** 子环约束策略：max(parent_review_level - 1, L0)
3. **[DL-003]** 任务卡片加载：从 task_card_path 加载验收标准

---

*版本：v3.1 | 创建：2026-04-05 | PM：张铁 | 状态：🔄 进行中*
