# TASK-CARD · T5.8

> **文件性质**：Task 执行卡片
> **版本**：v3.1
> **Task ID**：T5.8
> **Task 名称**：端到端验证
> **负责人**：张铁 (CQO)
> **状态**：✅ 已完成
> **完成日期**：2026-04-06

---

## 一、Task 概要

| 字段 | 内容 |
|------|------|
| **Task ID** | T5.8 |
| **Task 名称** | 端到端验证 |
| **负责人** | 张铁 (CQO) |
| **优先级** | P0 |
| **状态** | 🔄 进行中 |
| **Review 级别** | L3（Harold 全量 Review） |
| **依赖** | T5.7 |

---

## 二、验证目标

验证 NUCLEUS 4.0 Phase 1 完整流程：

1. **Skill 加载**：验证 governance-nucleus 可以被正确加载
2. **调度器触发**：验证 on_heartbeat() 正确执行
3. **PDCA 循环**：验证完整 Plan→Do→Check→Act 流程
4. **Monitor 集成**：验证状态感知正常
5. **文件持久化**：验证 CycleUnit 和日志正确写入

---

## 三、验证步骤

### Step 1：验证 Skill 加载

```python
# 验证 governance-nucleus 可被加载
import sys
sys.path.insert(0, '~/.openclaw/skills/openclaw-governance/skills/openclaw-governance-nucleus/scripts')

from scheduler import on_heartbeat
from monitor import sense_system_state
print("✅ Skill 加载成功")
```

### Step 2：验证调度器触发

```python
# 运行调度器
result = on_heartbeat()
assert 'triggered_scopes' in result
assert 'created_cycles' in result
print(f"✅ 调度器触发: {result['triggered_scopes']}")
```

### Step 3：验证 PDCA 循环

```python
# 验证 Plan
from plan import determine_review
review = determine_review(task_card_path, task_id, scope)
assert review['review_level'] in ['L0', 'L1', 'L2', 'L3']

# 验证 Do
from do import execute_cycle
do_result = execute_cycle(cycle_id)
assert do_result['status'] in ['completed', 'blocked', 'failed']

# 验证 Check
from check import check_cycle
check_result = check_cycle(cycle_id)
assert check_result['verdict'] in ['pass', 'partial', 'fail', 'skip']

# 验证 Act
from act import adjust_automation_level
adjustments = adjust_automation_level(cycle_id, verdict, count, level, evidence)
assert len(adjustments) > 0
print("✅ PDCA 循环正常")
```

### Step 4：验证 Monitor

```python
# 验证状态感知
snapshot = sense_system_state()
assert snapshot.active_cycles is not None
assert snapshot.log_entries is not None
print("✅ Monitor 正常")
```

### Step 5：验证文件持久化

```bash
# 检查 CycleUnit 文件
ls cycles/task/*.yaml

# 检查日志文件
ls logs/*.jsonl
ls executions/*.jsonl

# 检查配置文件
ls config/*.yaml
```

---

## 四、验证脚本

运行验证脚本：

```bash
cd /Users/haroldtsui/Workspaces/openclaw/main/10_Projects/ZT-P015_NUCLEUS-4-0
python3 demo_phase1.py
```

---

## 五、验收标准

| 标准 | 状态 | 说明 |
|------|------|------|
| **DoD-1** | ✅ | Skill 加载无错误 |
| **DoD-2** | ✅ | 调度器触发并创建 CycleUnit |
| **DoD-3** | ✅ | PDCA 循环完整运行 |
| **DoD-4** | ✅ | Monitor 返回有效数据 |
| **DoD-5** | ✅ | 文件持久化正确 |

---

## 六、执行记录

| 日期 | 事件 | 说明 |
|------|------|------|
| 2026-04-06 | T5.8 启动 | 创建验证 TASK-CARD |

---

## 七、知识沉淀

### 7.1 LESSON-LEARNED

1. **[LL-001]** 端到端验证需要覆盖完整流程：从 Skill 加载到文件持久化
2. **[LL-002]** 验证步骤需要结构化：分步骤验证，每个步骤有明确的目标
3. **[LL-003]** 验证脚本需要可执行：使用 Python 脚本实现自动化验证

### 7.2 DECISION-LIBRARY

1. **[DL-001]** 验证策略：覆盖完整流程，分步骤验证
2. **[DL-002]** 验证方法：使用 Python 脚本实现自动化验证
3. **[DL-003]** 验证标准：每个步骤有明确的验收标准

---

*版本：v3.1 | 创建：2026-04-06 | PM：张铁 | 状态：🔄 进行中*
