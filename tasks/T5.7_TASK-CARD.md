# TASK-CARD · T5.7

> **文件性质**：Task 执行卡片
> **版本**：v3.1
> **Task ID**：T5.7
> **Task 名称**：Heartbeat 集成
> **负责人**：张铁 (CQO)
> **状态**：✅ 已完成
> **完成日期**：2026-04-06

---

## 一、Task 概要

| 字段 | 内容 |
|------|------|
| **Task ID** | T5.7 |
| **Task 名称** | Heartbeat 集成 |
| **负责人** | 张铁 (CQO) |
| **优先级** | P0 |
| **状态** | 🔄 进行中 |
| **Review 级别** | L2（银月抽样） |
| **依赖** | T2.2, T5.5, T5.6 |

---

## 二、Task 目标

将 scheduler.py 集成到 governance-heartbeat 中：

1. **非侵入式集成**：不影响现有 heartbeat 巡检逻辑
2. **错误隔离**：调度器错误不影响原有巡检
3. **触发时机**：在 Step 3 执行推进后追加运行

---

## 三、核心接口

### 3.1 集成点

```python
# 在 governance-heartbeat 的 Step 3 后添加
def integrate_nucleus_scheduler():
    """NUCLEUS 4.0 调度器集成"""
    try:
        from core.scheduler import on_heartbeat
        result = on_heartbeat()
        # 记录调度结果
        log_scheduler_result(result)
    except Exception as e:
        # 错误隔离，不影响原有巡检
        log_scheduler_error(e)
```

### 3.2 日志记录

```python
def log_scheduler_result(result: Dict) -> None:
    """记录调度结果到 logs/"""

def log_scheduler_error(error: Exception) -> None:
    """记录调度错误到 logs/"""
```

---

## 四、交付物

- [x] `patches/nucleus_scheduler_integration.patch`（集成补丁）
- [x] 集成测试验证
- [x] 错误隔离验证

---

## 五、验收标准

| 标准 | 状态 | 说明 |
|------|------|------|
| **DoD-1** | ✅ | 调度器正确触发 |
| **DoD-2** | ✅ | 不影响现有 heartbeat 巡检 |
| **DoD-3** | ✅ | 错误隔离生效 |
| **DoD-4** | ✅ | 调度结果正确记录 |

---

## 六、执行流程

### Step 1：分析 governance-heartbeat 结构

### Step 2：创建集成补丁

### Step 3：验证集成效果

### Step 终：闭环

---

## 七、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 影响现有巡检 | 功能受损 | try-catch + 独立错误处理 |
| 调度器性能问题 | heartbeat 延迟 | 性能监控 + 超时控制 |

---

## 八、完成记录

| 日期 | 事件 | 说明 |
|------|------|------|
| 2026-04-06 | T5.7 启动 | 创建 TASK-CARD |
| 2026-04-06 | 集成脚本实现 | nucleus_scheduler_integration.py |
| 2026-04-06 | 集成测试验证 | 成功触发调度器 |
| 2026-04-06 | T5.7 完成 | 所有 DoD 达成 |

---

## 九、知识沉淀

### 9.1 LESSON-LEARNED

1. **[LL-001]** 集成需要非侵入式：不影响现有 heartbeat 巡检逻辑
2. **[LL-002]** 错误隔离很重要：调度器错误不影响原有巡检
3. **[LL-003]** 触发时机需要合适：在 Step 3 执行推进后追加运行

### 9.2 DECISION-LIBRARY

1. **[DL-001]** 集成策略：非侵入式集成，不影响现有功能
2. **[DL-002]** 错误隔离：try-catch 捕获异常，独立错误处理
3. **[DL-003]** 触发时机：在 Step 3 执行推进后追加运行

---

*版本：v3.1 | 创建：2026-04-06 | PM：张铁 | 状态：🔄 进行中*
