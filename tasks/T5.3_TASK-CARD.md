# TASK-CARD · T5.3

> **文件性质**：Task 执行卡片
> **存放路径**：`${OPENCLAW_LOCAL_WORKSPACE}/10_Projects/ZT-P015_NUCLEUS-4-0/tasks/T5.3_TASK-CARD.md`
> **版本**：v3.1
> **Task ID**：T5.3
> **Task 名称**：Act 模块实现
> **负责人**：张铁 (CQO)
> **状态**：✅ 已完成
> **完成日期**：2026-04-06

---

## 一、Task 概要

| 字段 | 内容 |
|------|------|
| **Task ID** | T5.3 |
| **Task 名称** | Act 模块实现 |
| **负责人** | 张铁 (CQO) |
| **优先级** | P0 |
| **状态** | ✅ 已完成 |
| **Review 级别** | L3（Harold 全量 Review） |
| **依赖** | T0.2, T1.1, T5.2 |

---

## 二、Task 目标

实现 Act 模块，负责 PDCA 循环的调整阶段：

1. **执行 adjustments**：调用 `apply_adjustments()` 执行变更
2. **调整自动化级别**：调用 `adjust_automation_level()` 计算级别变更
3. **传播到父环**：调用 `propagate_adjustments()` 传播调整
4. **Phase 屏障保护**：使用 `PhaseBarrierLock` 保证执行原子性

---

## 三、核心接口

### 3.1 adjust_automation_level() 调用

```python
def adjust_automation_level(
    cycle_id: str,
    verdict: str,
    consecutive_count: int,
    current_level: str,
    evidence: List[str]
) -> List[Dict[str, Any]]:
    """
    根据 ARCH v1.4.3 §3.3 计算自动化级别调整
    
    Args:
        cycle_id: CycleUnit ID
        verdict: 验收结果（pass/partial/fail/skip）
        consecutive_count: 连续计数
        current_level: 当前级别（L0/L1/L2/L3）
        evidence: 证据列表
    
    Returns:
        adjustments 列表（写入 cycle_unit.act）
    
    调整规则（schema v1.3-fixed constraints）:
        pass（连续≥3次）→ 升级（逐级，L2→L3 须 Harold 确认）
        partial           → 维持
        fail（连续≥2次）→ 降级（逐级，L1-L3 可降，L0 不可降）
        skip              → 维持（consecutive_count 不变）
    """
```

### 3.2 PhaseBarrierLock 执行保障

```python
class PhaseBarrierLock:
    """
    Phase 屏障锁（ARCH v1.4.3 §3.3.2）
    
    职责：
    - 保证 act 阶段执行的原子性
    - 执行前备份原文件
    - 失败时自动回滚
    - 验证变更是否生效
    
    使用示例：
        with PhaseBarrierLock(cycle_path) as lock:
            lock.backup()
            apply_adjustments(adjustments)
            lock.verify()
    """
```

### 3.3 propagate_adjustments() 传播

```python
def propagate_adjustments(
    cycle_id: str,
    adjustments: List[Dict[str, Any]],
    propagate_to: str = 'self'
) -> None:
    """
    传播调整到父环（ARCH v1.4.3 §2.3）
    
    Args:
        cycle_id: CycleUnit ID
        adjustments: 调整列表
        propagate_to: 传播方向（self|parent|both）
    
    规则：
        - propagate_to=parent → 写入父环 act.incoming_adjustments
        - propagate_to=both → 同时写入 self 和 parent
    """
```

---

## 四、交付物

- [x] `modules/act.py`（Act 模块实现）
- [x] `modules/test_act.py`（单元测试）
- [x] adjust_automation_level() 实现
- [x] PhaseBarrierLock 实现
- [x] propagate_adjustments() 实现
- [x] TASK-CARD 状态更新

---

## 五、验收标准

| 标准 | 状态 | 说明 |
|------|------|------|
| **DoD-1** | ✅ | `adjust_automation_level()` 正确计算级别变更 |
| **DoD-2** | ✅ | `PhaseBarrierLock` 保证执行原子性 |
| **DoD-3** | ✅ | 执行前备份、失败回滚、验证生效 |
| **DoD-4** | ✅ | `propagate_adjustments()` 正确传播到父环 |
| **DoD-5** | ✅ | L0 不可降、L2→L3 须 Harold 确认 |
| **DoD-6** | ✅ | verdict=skip 时写入 type=maintain 记录 |
| **DoD-7** | ✅ | 单元测试 16/16 passed (100%) |

---

## 六、执行流程

### Step 1：读取 ARCH v1.4.3 §3.3 接口规范（已完成）

确认：
- adjust_automation_level() 接口
- PhaseBarrierLock 执行保障
- propagate_adjustments() 传播规则
- 调整规则（schema constraints）

### Step 2：实现 adjust_automation_level()

```python
# modules/act.py

def adjust_automation_level(...) -> List[Dict[str, Any]]:
    # 1. 判断 verdict 类型
    # 2. 计算 consecutive_count 变化
    # 3. 应用级别调整规则
    # 4. 特殊约束检查（L0 不可降、L2→L3 须确认）
    # 5. 返回 adjustments 列表
```

### Step 3：实现 PhaseBarrierLock

```python
class PhaseBarrierLock:
    def __init__(self, cycle_path: str):
        self.cycle_path = cycle_path
        self.backup_path = None
        self.lock = threading.Lock()  # 线程安全
    
    def backup(self) -> None:
        # 备份原文件到 .backup
    
    def rollback(self) -> None:
        # 从备份恢复
    
    def verify(self) -> bool:
        # 验证变更生效
```

### Step 4：实现 propagate_adjustments()

```python
def propagate_adjustments(...) -> None:
    # 1. 查找父环 cycle_unit
    # 2. 写入 act.incoming_adjustments
    # 3. 更新 propagate_to 字段
```

### Step 5：单元测试验证

- 级别调整正确
- Phase 屏障生效
- 传播规则正确
- 特殊约束生效

### Step 终：闭环

1. 更新 T5.3 TASK-CARD 状态 ✅
2. 更新 TOPIC-BRIEF Task 列表 ✅
3. 测试覆盖率 100% ✅

---

## 七、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| Phase 屏障锁线程安全问题 | 并发损坏 | threading.Lock() + 原子写入 |
| L2→L3 升级未获确认 | 级别失控 | 确认标记 + 拒绝写入 |
| 父环不存在时传播失败 | 调整丢失 | 容错处理 + 告警日志 |

---

## 八、完成记录

| 日期 | 事件 | 说明 |
|------|------|------|
| 2026-04-06 | T5.3 启动 | 创建 TASK-CARD |
| 2026-04-06 | act.py 实现 | adjust_automation_level + PhaseBarrierLock + propagate_adjustments |
| 2026-04-06 | test_act.py 实现 | 16 个单元测试 |
| 2026-04-06 | T5.3 完成 | 所有 DoD 达成，16/16 tests passed |

---

## 九、知识沉淀

### 9.1 LESSON-LEARNED

1. **[LL-001]** 调整阶段需要保证原子性：使用 PhaseBarrierLock 保证 act 阶段执行的原子性，防止并发损坏
2. **[LL-002]** 自动化级别调整需要特殊约束：L0 不可降，L2→L3 须 Harold 确认
3. **[LL-003]** 调整需要传播到父环：调用 propagate_adjustments() 传播调整，确保整个系统的一致性

### 9.2 DECISION-LIBRARY

1. **[DL-001]** 调整规则：根据 verdict 和 consecutive_count 计算自动化级别调整
2. **[DL-002]** 执行保障：使用 PhaseBarrierLock 保证执行原子性
3. **[DL-003]** 传播策略：传播到 self、parent 或 both

---

*版本：v3.1 | 创建：2026-04-06 | PM：张铁 | 状态：🔄 进行中*
